"""I/O helpers — FITS reads/writes, headers, calibration text formats.

Submodules:
    * :mod:`decanter.io.fits` — FITS read/write preserving headers + WCS helpers.
    * :mod:`decanter.io.headers` — WINERED-specific header keyword extraction.
    * :mod:`decanter.io.fsr` — parse ``FSR_winered_*.txt`` (Free Spectral Range table).
    * :mod:`decanter.io.linelists` — parse ThAr linelists.
    * :mod:`decanter.io.apdb` — parse IRAF ``database/ap*`` aperture files.
    * :mod:`decanter.io.parameter` — parse WARP-style ``Key: value`` parameter files.
"""
